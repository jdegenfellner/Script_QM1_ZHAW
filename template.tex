\documentclass[12pt]{book}
\usepackage{graphicx}

\begin{document}

% Add the PNG cover image
\begin{center}
\includegraphics[width=\textwidth]{images/CoverQM1.png}
\end{center}

% Insert title, author, and date from YAML
$if(title)$
\title{$title$}
$endif$

$if(author)$
\author{$for(author)$$author$$sep$ \\ $endfor$}
$endif$

$if(date)$
\date{$date$}
$endif$

\maketitle

\end{document}